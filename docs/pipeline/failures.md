# Pipeline failure log

Every escalation in the pipeline is recorded here. Failures escalate upstream:
**execution → specification → documentation**. See the [agent pipeline diagram](../diagrams/agent-pipeline.svg).

| Class | Meaning | Routed to |
|---|---|---|
| CODE | Implementation defect (test fails, reviewer finds a bug) | implementer (max 3 attempts per task) |
| SPEC | 3 failed attempts, or reviewer finds a spec gap | spec-writer |
| DOC | Business rule missing or contradictory, or 2 spec revisions failed | analyst → human decision |
| ENV | Infrastructure, API outage, credentials — not attributed to the spec | human |

## Incidents

| # | Date | Task | Class | Attempts | Root cause | Artifact changed | Resolution |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-24 | Spec (phase 0) | DOC | — | BR-37/BR-38 ambiguous (can a cookie for one RSVP edit another RSVP's name?); no rule for "Fill with AI" on non-event text (BR-55 vs BR-59) | business-rules.md (BR-37, BR-38, BR-55 amended; BR-96 new) → spec.md, plan.md | Human decided: cookie only authorizes its own RSVP (DUPLICATE_NAME); non-event text → all fields missing + message "Couldn't find event details in that text." |
| 2 | 2026-09-24 | TASK-02, TASK-03 | CODE (process) | 1 each | Implementer ran Prettier / produced files but left formatting uncommitted; `format:check` passed locally, would fail on a clean checkout | `.claude/agents/implementer.md` (format:check + clean `git status` required per task) | Orchestrator committed the formatting (`style:` commits); rule added so it cannot recur |
| 3 | 2026-09-24 | TASK-10, TASK-11 | ENV | — | Port 3000 occupied by an unrelated local application; Playwright could silently test the wrong app | plan.md (TASK-10, TASK-11, HUMAN-04 via spec-writer; human-decision revision, not a failure revision) | Human chose a configurable `E2E_PORT` (3100 locally, 3000 in CI); independent traceability batch run ahead meanwhile |
| 4 | 2026-09-24 | TASK-01…TASK-19 (dependencies) | CODE | 1 | `package-lock.json` generated with npm 11 (local Node 24); CI's npm 10 (Node 22) resolves an optional peer (`@swc/helpers` under `next-intl`) differently, so `npm ci` failed in every CI job. Hidden until the first CI run | `package-lock.json` regenerated with npm 10 and validated with `npm ci` on npm 10 and 11; implementer rule added | Reviewer round 1 finding #1 |
| 5 | 2026-09-24 | TASK-03 | CODE | — | Commit body line of 344 chars (Haiku); the local commit-msg hook only arrived in TASK-18 | Commit reworded (history rewrite on PR branch, authorized by human; tree unchanged) | Reviewer round 1 finding #2 |
| 6 | 2026-09-24 | TASK-02 | CODE | — | `commitlint.config.mjs` changed outside the task's file list | None — accepted as justified deviation (required for lint to pass) | Reviewer round 1 finding #3 |
| 7 | 2026-09-24 | Repository | ENV | — | Windows `core.autocrlf=true` re-materialized LF files as CRLF after a reset; local `format:check` flagged 33 files (CI unaffected) | `.gitattributes` (`* text=auto eol=lf`) | Found by orchestrator while verifying the lockfile fix |
| 8 | 2026-09-24 | TASK-20, HUMAN-01 | ENV | — | Vercel project created without an initial deployment; a deployment of `main` created by hand in the dashboard was not flagged production, so the Ignored Build Step `[ "$VERCEL_ENV" != "production" ]` canceled it | plan.md (TASK-20 Rev 2, HUMAN-01 steps 1 and 6 via spec-writer; human-decision revision, not a failure revision) + vercel.json | Human decided a branch-based ignore step `[ "$VERCEL_GIT_COMMIT_REF" != "main" ]`: per-PR previews still skipped, any deployment of `main` builds; the merge to `main` triggers the production deploy |
| 9 | 2026-09-24 | TASK-39, TASK-40, TASK-42 | SPEC (self-resolved) | 1 | Plan ordering gap: TASK-39/40 need `EventRecord` (Contract C2) but `src/domain/types.ts` was assigned to TASK-42 | None in docs; implementer created the file early, verbatim from C2 | No escalation needed; lesson for spec-writer: create shared types before first use |
| 10 | 2026-09-24 | TASK-03 → TASK-60 | SPEC (self-resolved) | 1 | Plan did not pin `vite`/`@vitejs/plugin-react`; two vite majors installed, JSX in component tests broke at the first component test. `@testing-library/jest-dom` not in the dependency list | package.json (vite ^7, plugin-react ^4), lockfile validated with npm 10 | Lesson for spec-writer: pin toolchain peers in scaffolding tasks |
| 11 | 2026-09-24 | TASK-65 | SPEC (self-resolved) | 1 | Test fixture in the plan (GOING 2 / NOT_GOING 0) contradicted the task's expected summary (Going 1 · Declined 1 · People 2) | Test data chosen to match the stated expectation and `computeTotals` semantics | Lesson for spec-writer: derive expected values from fixtures mechanically |
| 12 | 2026-09-24 | HUMAN-02 | ENV | — | Google requires full branding (home page, privacy policy, verified authorized domain) to publish an External OAuth app; `vercel.app` ownership cannot be proven | None — human decision: keep the OAuth app in Testing and add the evaluator's Google account as a test user | Documented in README "What I left out" |
| 13 | 2026-09-24 | TASK-78 (+ TASK-79) | SPEC | 1 | Tests written verbatim from the plan passed before any implementation: the service-level duplicate-name pre-check (algorithm step 6) was unreachable because RsvpRepository.create/update already throw DuplicateNameError (Contract C4 + DB unique constraint). The implementer refused to commit a green "red" test and returned SPEC_FAILURE | plan.md: TASK-76 rev 2 (step 6 removed), TASK-78 rev 2 (characterization; SPEC failure revision 1/2), TASK-79 rev 2 (boundary test that fails first), TASK-86/89 dependency notes; spec.md REQ-26 states the enforcement point | spec-writer also flagged TASK-140/142 (Phase 5) with the same pattern — to fix before Phase 5 |
| 14 | 2026-09-24 | TASK-143 | CODE | 1 | RsvpForm computed a form-level error (`fieldErrors.form`, e.g. honeypot rejection) but never rendered it: silent dead end for a real guest whose browser autofilled the honeypot | rsvp-form.tsx renders the form-level alert (test first) | Reviewer PR #8 round 1 |
| 15 | 2026-09-24 | TASK-143 | SPEC | — | The spec mapped every VALIDATION_ERROR to "Please fix the highlighted fields." with no case for form-level errors, where no field is highlighted | spec.md (error table, REQ-58), plan.md TASK-143 rev 3 (SPEC failure revision 1/2), new key `rsvp.formRejected` in en/fr/pt-BR | Reviewer PR #8 round 2; spec-writer then implementer |
| 16 | 2026-09-25 | TASK-175 | CODE (self-resolved) | 1 | Stepper buttons measured 42 px instead of 44 px (box-sizing on the container ate 2 px) — BR-109 | globals.css (`fix(ui)` commit) | Found by the implementer while writing the target-size test |
| 17 | 2026-09-25 | TASK-182 | CODE | 1 | Characterization test found no visible focus ring on date/time inputs: Chromium focuses an internal picker sub-part that matches neither `:focus` nor `:focus-visible` (BR-103) | globals.css hides the WebKit clear/picker sub-parts (`fix(ui)` after the failing test) | Declared characterization caught a real accessibility defect |
| 18 | 2026-09-25 | TASK-217 | SPEC (eval outcome) | — | Real eval: only claude-sonnet-5 passes the gate; the code default (claude-haiku-4.5) fails must-not-invent (75%) | plan.md TASK-218; providers-config.ts default models | Code default follows the eval so production needs no extra Vercel variable |
| 19 | 2026-09-25 | TASK-150 | CODE (repeat of #4) | 1 | After an EPERM while running `npm@10 ci` in the working directory, `node_modules` was repaired with a plain local `npm install` (npm 11), which rewrote `package-lock.json` and dropped the nested optional peer npm 10 requires; rule 8 validated before the repair, not after. CI failed on PR #10 | lockfile regenerated with npm 10 in a scratch clone (validated with npm 10 and npm 11); implementer rule 10 | Root cause is npm 11 locally vs npm 10 in CI; recommended permanent fix (human decision): align the CI npm version with local |
| 20 | 2026-09-25 | PR #12 (final docs) | DOC (process) | 4 review rounds | The README's "verified by hand" list was derived from timelog events that did not record who performed each check (human or orchestrator); the reviewer correctly rejected claims it could not attribute. Exceeded the 2-round review budget | timelog events now name the actor; README separates hand-verified checks from orchestrator smoke tests; `timelog` skill requires an actor on every event | Human approved the final README and the merge |
| 21 | 2026-09-25 | TASK-234 | ENV | 1 | Local Node 24.16 on Windows aborts a child process on exit (libuv assertion `!(handle->flags & UV_HANDLE_CLOSING)`, async.c:94) after two sequential `fetch` calls; the spawned-runner test could not pass locally. `.nvmrc` and CI pin Node 22 | None in code; the orchestrator verified the implementation under a temporary Node 22 (`npx -p node@22`) and committed it | Recommended permanent fix (human): install Node 22 locally (e.g. nvm-windows or volta), which also brings npm 10 and removes the root cause of incidents #4 and #19 |

