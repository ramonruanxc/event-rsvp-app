/** Builds an event's invite URL, with no locale prefix: the guest's locale is detected on arrival (REQ-38). */
export function buildInviteUrl(_origin: string, _slug: string): string {
  throw new Error('not implemented');
}
