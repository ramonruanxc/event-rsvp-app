/** Joins truthy class name parts with a single space; falsy parts are skipped. */
export function cx(...parts: Array<string | false | null | undefined>): string {
  throw new Error('not implemented');
}
