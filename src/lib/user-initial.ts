/** Returns the avatar initial from the user's name, falling back to their email or `'?'` (REQ-80). */
export function userInitial(_name: string | null, _email: string | null): string {
  throw new Error('not implemented');
}
