/** Recursively lists the sorted, dot-joined leaf keys of a messages object. */
export function flattenKeys(_messages: Record<string, unknown>, _prefix = ''): string[] {
  throw new Error('not implemented');
}
