'use server';

/** Sets or changes the signed-in user's password (REQ-120). */
export async function setPasswordAction(): Promise<never> {
  throw new Error('not implemented');
}

/** Dismisses the cleared-password notice of the signed-in user (REQ-123). */
export async function dismissPasswordNoticeAction(): Promise<never> {
  throw new Error('not implemented');
}
